import InfoPageShell from "@/components/InfoPageShell";

export default function TransparencyPage() {
  return (
    <InfoPageShell title="Ledger">
      <p>
        Every proof and penalty in an arena is logged in one place. Members can
        scroll the history, verify submissions, and see who kept their streak.
      </p>
      <p>
        There are no hidden adjustments — what the group sees in the ledger is
        what happened.
      </p>
    </InfoPageShell>
  );
}
